{
  "name": "build",
  "private": true,
  "version": "1.0.0",
  "main": "input.js",
  "type": "module",
  "devDependencies": {
    "micro-rsa-dsa-dh": "file:..",
    "esbuild": "0.25.3"
  },
  "scripts": {
    "build:release": "npx esbuild --bundle input.js --outfile=`npx jsbt outfile` --global-name=`npx jsbt global`"
  }
}
